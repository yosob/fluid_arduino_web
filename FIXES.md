# 🔧 常见问题快速修复

## 问题 1: 图标导入错误 ✅ 已修复

**错误信息:**
```
The requested module does not provide an export named 'VideoStop'
```

**原因:**
Element Plus Icons 中没有 `VideoStop` 图标。

**解决方案:**
已将 `VideoStop` 替换为 `CircleClose`。

**修改文件:**
- `src/components/LoopManager.vue`

---

## 问题 2: Web Serial API 不可用

**错误信息:**
```
serial is not defined
```

**解决方案:**
1. 确认使用 Chrome 或 Edge 浏览器
2. 确认通过 `https://` 或 `http://localhost` 访问
3. 更新浏览器到最新版本

---

## 问题 3: npm install 失败

**错误信息:**
```
npm ERR! code ERESOLVE
```

**解决方案:**
```bash
# 清除缓存
npm cache clean --force

# 重新安装
npm install

# 如果还是失败，使用 --legacy-peer-deps
npm install --legacy-peer-deps
```

---

## 问题 4: 端口被占用

**错误信息:**
```
Port 5173 is already in use
```

**解决方案:**
```bash
# 方案 1: 修改端口
npm run dev -- --port 5174

# 方案 2: 关闭占用端口的进程
# Windows:
netstat -ano | findstr :5173
taskkill /PID <进程ID> /F

# Linux/Mac:
lsof -ti:5173 | xargs kill -9
```

---

## 问题 5: 串口权限被拒绝

**错误信息:**
```
Failed to execute 'requestPort' on 'SerialPort'
```

**解决方案:**
1. 确认 USB 设备已正确连接
2. 关闭其他使用串口的程序（Arduino IDE、串口助手等）
3. 重新插拔 USB 线
4. 检查浏览器是否授予了串口访问权限

---

## 📝 其他注意事项

### 开发环境
- Node.js 版本 >= 18.0.0
- npm 版本 >= 9.0.0
- 推荐使用 Chrome 或 Edge 浏览器

### 调试技巧
1. 打开浏览器控制台（F12）查看错误信息
2. 使用 Arduino IDE 串口监视器查看下位机日志
3. 启用 Vite 的 HMR（热更新）加快开发速度

### 性能优化
- 生产环境使用 `npm run build` 构建
- 启用 Gzip 压缩
- 使用 CDN 加速静态资源

---

**最后更新:** 2025-01-14
